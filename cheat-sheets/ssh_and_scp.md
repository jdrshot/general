# SSH & SCP

## SSH

SSH to remote server
```
ssh <ip> or ssh <hostname>
```

Specify username
```
ssh <user>@<ip> or ssh <user>@<ip>
```

Use different port
```
ssh <ip> -p <port>
```

Generate SSH key
```
ssh-keygen -t rsa
```